/**
 * Version of the `scaffold.io` dependency, inlined at build time by Vite's
 * `define` (see vite.config.ts). Used to pin the hero runner's import map to
 * the same version that's installed.
 */
declare const __SCAFFOLD_VERSION__: string;
