import React from 'react';
import Context from '../sbl/Context.ts';
import { BlockExt } from '../sbl/BlockMeta.ts';

export default ({ ctx, block }: { ctx: Context; block: BlockExt }) => {
  return <div></div>;
};
